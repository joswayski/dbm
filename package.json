{
  "name": "dbv",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "dev": "npm run tauri:dev --workspace @dbv/desktop",
    "build": "npm run build --workspace @dbv/desktop",
    "check": "npm run check --workspace @dbv/desktop",
    "test": "npm run test --workspace @dbv/desktop",
    "tauri:dev": "npm run tauri:dev --workspace @dbv/desktop",
    "tauri:build": "npm run tauri:build --workspace @dbv/desktop"
  }
}
